// for to use http protocol
const http = require('http');

// to call app.js file need to require it
const app = require('./app');

// port number to call the api on if environment is set then user env.PORT else 3000
const port =  3001;

// create a server on the http to listen app files.
const server = http.createServer(app);

server.listen(port);


const express = require('express');
const router = express.Router();
const upload  = require('../../../config/multer.upload');


let uploaderController = require('../../../controllers/uploader.controller');

// router.post('/imageUpload/',uploadImage.single('originalFileName'),uploaderController.uploadFile);
router.post('/imageUpload/',upload.array('originalFileName',10),uploaderController.uploadFile);

module.exports = router; 

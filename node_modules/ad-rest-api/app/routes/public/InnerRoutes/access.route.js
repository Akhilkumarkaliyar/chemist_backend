const express = require('express');
const router = express.Router();
let accessController = require('../../../controllers/access.controller');
router.get('/',accessController.isValidLogin);
router.post('/',accessController.post);
module.exports = router; 
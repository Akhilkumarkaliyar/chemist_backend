const express = require('express');
const router = express.Router();
let studentsController = require('../../../../controllers/students/student.controller');

// let upload = require('../../../../config/multer.upload');
router.post('/',studentsController.postRegistration);
router.get('/adhar/',studentsController.getByAdhar);

module.exports = router; 
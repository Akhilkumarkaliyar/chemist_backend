const express = require('express');
const router = express.Router();
let helperController = require('../../../controllers/helper.controller');
router.get('/',helperController.getServerDate);
module.exports = router; 
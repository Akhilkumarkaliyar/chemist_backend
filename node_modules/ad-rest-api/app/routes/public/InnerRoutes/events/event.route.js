
const express = require('express');
const router = express.Router();
let eventsController = require('../../../../controllers/events/event.controller');

router.get('/',eventsController.get);

let upload = require('../../../../config/multer.upload');
router.post('/', upload.single('originalFileName'),eventsController.postEvent);

module.exports = router; 
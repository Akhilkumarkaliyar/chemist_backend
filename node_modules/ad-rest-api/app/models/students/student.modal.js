const joi = require("joi");

module.exports = {
  body: {
    idStudents :joi.number().allow("").allow(null).default(null),
    firstNameStudents: joi.string().required(),
    middleNameStudents: joi.string().required(),
    lastNameStudents: joi.string().required(),
    emailStudents: joi.string().required(),
    adharNoStudents: joi.number().required(),
    mobileNoStudents: joi.number().required(),
    genderStudents: joi.string().required(),
    dobStudents: joi.date().required(),
    coachNameStudents: joi.string().required(),
    coachMoNoStudents: joi.number().required(),
    addressStudents: joi.string().required(),
    imageStudents: joi.string().required(),
    isFromEvent:joi.number().required(), 
    eventId :joi.number().allow("").allow(null).default(null), 
    ageGroup: joi.number().required() 
  }
};
